[build]
  command = "pip install -r requirements.txt"

[deploy]
  startCommand = "python railway-entry.py"
  healthcheckPath = "/"
  healthcheckInterval = 60
  healthcheckTimeout = 10
  healthcheckRetries = 3
